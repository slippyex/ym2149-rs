{
  "name": "ym2149-player",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && mv ../player/index.dev.html ../player/index.html && cp catalog*.json* ../player/ && cp -r collections/* ../player/ && mkdir -p ../player/pkg && cp pkg/ym2149_wasm.js pkg/ym2149_wasm_bg.wasm ../player/pkg/",
    "preview": "vite preview --outDir ../player",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf ../player"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.12",
    "terser": "^5.27.0",
    "tailwindcss": "^3.4.1",
    "postcss": "^8.4.33",
    "autoprefixer": "^10.4.17"
  }
}
